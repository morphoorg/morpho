#!/bin/python

from morpho.utilities import morphologging, toolbox
logger=morphologging.getLogger(__name__)

myToolBox = toolbox.ToolBox("myConfigFile.yaml")
myToolBox.Run()
# myToolBox._CreateAndConfigureProcessors()