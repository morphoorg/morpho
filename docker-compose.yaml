version: "3"

services:
  morpho:
    build: .
    command: bash
    environment:
      - PYTHONPATH=/usr/local/root/lib/root
    volumes:
      # share a subdirectory from the host to /host in the docker (can be edited)
      - ~/morpho_share:/host
